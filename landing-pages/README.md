# landing-pages/

Landing pages publicadas ou em producao, geradas pela skill `landing-page`. Se uma landing page pertence a um produto especifico com outros materiais (imagens, mockups), prefira guarda-la em `produtos/<produto>/landing-page/` em vez de aqui.
